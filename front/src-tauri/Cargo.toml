[package]
name = "aresta"
version = "1.0.0"
description = "Aresta — Leitor Digital Local-First & Retenção de Conhecimento"
authors = ["Aresta Team"]
edition = "2021"

[lib]
name = "aresta_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-sql = { version = "2", features = ["sqlite"] }
tauri-plugin-fs = "2"
tauri-plugin-dialog = "2"
tauri-plugin-shell = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
