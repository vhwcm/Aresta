================================================================================
FLUXO DO LEITOR 2D NATIVO (READER 2D NATIVE ENGINE FLOW)
================================================================================

                    ┌──────────────────────────────────────┐
                    │       Página do Leitor (Vue 3)       │
                    │   - Container responsivo High-DPI    │
                    │   - Controles de 1 pág / 2 págs      │
                    └──────────────────┬───────────────────┘
                                       │
                                       │ 1. getPage(num, dpr)
                                       ▼
                    ┌──────────────────────────────────────┐
                    │    Canvas 2D Viewport (`<canvas>`)   │
                    │  - Dimensões físicas = cssW * dpr    │
                    │  - Escala ctx.scale(dpr, dpr)        │
                    │  - Desenha diretamente o Bitmap      │
                    └──────────────────┬───────────────────┘
                                       │
                                       │ 2. renderTextLayer(...)
                                       ▼
                    ┌──────────────────────────────────────┐
                    │  Camada de Texto DOM (`.text-layer`) │
                    │  - Spans de texto invisíveis         │
                    │  - Alinhamento exato de seleção      │
                    │  - Suporte a highlight & anotação    │
                    └──────────────────────────────────────┘
================================================================================
